const StatusView = ({ message }) => (
  <div className="rounded-2xl border border-white/70 bg-white/90 p-8 text-center text-sm font-medium text-slate-600 shadow-sm">
    {message}
  </div>
);

export default StatusView;
